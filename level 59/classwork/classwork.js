getGrade (s1, s2, s3) {
    let = getGrade(prompt('Enter Your getGrade'))
    let getGrade = (getGrade >= 'A') ? 'You get A' :
                   (getGrade >= 'B') ? 'You get B' :
                   (getGrade >= 'C') ? 'You get C' :
                   (getGrade >= 'D') ? 'You get D' :
                   (getGrade >= 'F') ? 'You get F' :
    }